CEF-browserd -- embeddable browser server
=========================================

A little CEF-based browser server, allowing to create browser
windows embedded in other X11 windows.

The browserd is controlled via trivial HTTP interface.
